{
  "name": "living-earth-trend-detective",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm run build",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && pip install -r requirements.txt",
    "data:generate": "cd backend && python scripts/generate_dummy_data.py"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
